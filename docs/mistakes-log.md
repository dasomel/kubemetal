# Mistakes Log (Compounding Engineering)

> Add a row whenever a mistake is made (including ones caught in review). Same mistake,
> never twice. Format: `| YYYY-MM-DD | Mistake | Fix |`
> Read the section matching your work area BEFORE touching that area
> (rule in `CLAUDE.md` → Mistakes Log).

## Rust / Tauri

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-07-20 | tauri.conf.json set `devUrl` but no `beforeDevCommand` — `tauri dev` waited forever for a vite server nobody started; cargo check/tsc/pnpm build all passed, only a real launch exposed it | Pair `devUrl` with `beforeDevCommand: "pnpm dev"` (and `beforeBuildCommand`); a config that references another process must also say who starts it |
| 2026-07-20 | `plugins: {"dialog": {}}` in tauri.conf.json — tauri-plugin-dialog takes NO config (unit type), so the empty map panicked PluginInitialization at launch; another compile-clean/launch-dead case | Plugins without config must not appear in `plugins` at all; registration = Rust `.plugin(init())` + capability permission, conf entry only for plugins that document config keys |
| 2026-07-20 | Parsed `colima status` by matching stdout strings — colima logs via logrus to **stderr**, so the match always fails; `vm_type` was also hardcoded | Use `colima status --json` and deserialize with serde |
| 2026-07-20 | `ColimaStatusRaw` guessed the JSON schema (`status: "Running"` + nested `kubernetes.enabled`) — real colima 0.10.3 output is a FLAT object with `kubernetes: bool` and NO `status` field; when stopped there's no stdout at all (exit 1), so the strict struct made a running cluster report STOPPED | Running = "exit 0 + JSON parses"; map only fields verified against real output (`kubernetes: bool` with `#[serde(default)]`); never ship a serde mapping for an external CLI without capturing its actual output first |
| 2026-07-20 | `Command::new("colima")` assumed shell PATH — packaged .app launched from Finder does NOT inherit it, so Homebrew CLIs are not found | Resolve absolute paths (`/opt/homebrew/bin`, `/usr/local/bin`, …) via `resolve_cli_path` helper before spawning |
| 2026-07-20 | Blocking `std::process::Command::output()` inside an async `tauri::command` (`colima start` runs minutes) | Use `tokio::process::Command`; never block the async runtime |
| 2026-07-20 | Error UX via JS `alert()` — wry WebView does not implement alert/confirm/prompt | Use `@tauri-apps/plugin-dialog` (`message`/`ask`) + `dialog:default` capability |
| 2026-07-20 | Created `sysinfo::System::new_all()` on every metrics call | Hold one instance in `tauri::State<Mutex<System>>` and `refresh_*()` per call |
| 2026-07-20 | External endpoint links rendered as `<a href>` — Tauri WebView cannot open external URLs via anchor/window.open, so the buttons silently did nothing | Register `tauri-plugin-opener` + `opener:allow-open-url` capability (scoped to `http://localhost:*`) and call `openUrl()`; browser-preview fallback `.catch(() => window.open(url, '_blank'))` |
| 2026-07-24 | `Command::new("ioreg")` shipped bare (no path resolve, no PATH env) by a session that hadn't read this log — the D5 class regressed: bundled app would silently report GPU 0% forever | Every external spawn goes through `external_command()`; broad `except Exception` around a missing `import subprocess` in ingest_host.py likewise silently killed the DVC backup node — never swallow exceptions without logging the error string |
| 2026-07-24 | `prefect.rs::start_prefect_runner` checked `runner_pid.is_some()` in one lock scope, then spawned, then set the PID in a second lock scope — a TOCTOU window where two concurrent calls could both pass the check and double-spawn the runner | Insert a sentinel (`Some(0)`) inside the same lock as the check so check-and-set is atomic (mirrors `mlx.rs::run_mlx_finetune`'s prev-value-capture + rollback-on-spawn-failure pattern) |
| 2026-07-24 | `ingest_host.py::fetch_url_bytes` retried failed HTTPS fetches with `ssl._create_unverified_context()` — any TLS verification failure (including a real MITM) silently downgraded to no verification instead of failing | Removed the fallback entirely; TLS verification is never bypassed. Also added scheme allowlist(http/https)+private-network host denylist for web/rss sources (D21), double-checked on the Rust side too (`data_ingest.rs::validate_ingest_url`) since the script can run standalone | 
| 2026-07-24 | DVC S3 credentials were hardcoded as CLI args (`--access-key seaweedfsadmin` in `rag.rs`/`rag_host.py`, and a same-named argparse default baked into `ingest_host.py` that the Rust caller never even overrode) — process args are visible via `ps`, and `"seaweedfsadmin"` didn't match the actual `seaweedfs-s3-credentials.yaml` Secret values (`kubemetal`/`kubemetal-local`) | Credentials now flow as `KUBEMETAL_S3_ACCESS_KEY`/`KUBEMETAL_S3_SECRET_KEY` env vars, sourced from the real K8s Secret via `access.rs::resolve_s3_credentials()` (falls back to the Secret manifest's own dummy values if the cluster/secret is unreachable) — never CLI args, never a value disconnected from the actual Secret (D13/D21) |
| 2026-07-25 | `get_hardware_spec` called blocking `std::process::Command` four times inside an async `tauri::command` (D7 violation) with bare binary names (`sysctl`, `system_profiler`) — the exact PATH-assumption regression D2 was amended for — and papered over every failure with hardcoded `Apple M4 Pro`/14/20/64GB, so any non-M4-Pro machine would have been shown someone else's specs as fact | `external_command` + `tokio` for every spawn; a failed probe returns `Err` (or `Option::None` for the optional GPU-core field), never a plausible-looking constant. Cache the static result in a `Mutex<Option<_>>` instead of doing the slow `system_profiler` call per invocation (D22) |
| 2026-07-25 | `get_kagent_diagnostics` derived `kagent_ready` from `!items.is_empty()` — a namespace full of CrashLoopBackOff pods reported "ready" — injected `["k8s-agent","helm-agent"]` into `active_agents` whenever the real list came back empty, and printed a fixed "이미지 태그를 찾을 수 없음" for every container `waiting.reason`; `list_kubeconfig_contexts` invented `["colima","narwhal-idp"]` on failure and `toggle_kagent_agent` claimed "파드가 1/1 Running" straight after `kubectl apply` (and swallowed delete failures as `Ok`) | Readiness = pods exist AND all have `Ready` condition `True`; agent names come only from Ready pods' `app.kubernetes.io/name` labels; cause strings quote the cluster's own `reason`/`message`; every probe failure propagates as `Err`. A command reports only what the command it ran actually returned (D22) |
| 2026-07-21 | Built the release executable with plain `cargo build --release` — without the `custom-protocol` feature the binary serves from `devUrl` (localhost:5173) instead of embedded assets, so it launches to a blank window when no dev server runs (binary literally contains the 5173 URL); process-alive smoke tests never catch this | Always build executables via the tauri CLI (`pnpm tauri build --no-bundle` for bin-only); verified fixed binary is byte-identical to the CLI-built .app binary |

## macOS platform

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-07-20 | Bound MLflow to host port 5000 — macOS AirPlay Receiver (ControlCenter) occupies 5000/7000, forwarding silently breaks | Use 5001 (D1); never claim 5000 on macOS |
| 2026-07-20 | Spec required "powermetrics C-Binding" for GPU metrics — powermetrics is a CLI (no public C API) and requires **root** | Phase 1 ships sysinfo RAM/CPU only; GPU metrics = Phase 3 privileged helper + CLI parsing (D2) |
| 2026-07-20 | OOM guard triggered on "available RAM < 10%" — macOS file cache keeps RAM near-full at all times, guaranteeing false triggers | Trigger on memory pressure levels (warn/critical) instead (D11) |
| 2026-07-21 | Switched serving URLs from `localhost` to `127.0.0.1` but the opener capability allowlist still only permitted `http://localhost:*` — Tauri silently denied every `openUrl("http://127.0.0.1:…")`, the `window.open` fallback is a no-op in wry, so link clicks did nothing (user-reported twice before root-caused) | A URL host change must update `capabilities/*.json` allowlists in the same commit; grep capabilities for the old host whenever changing endpoint hosts/schemes |
| 2026-07-21 | Serving links/health used `localhost` — macOS resolves it to ::1 first, and an unrelated IPv6 listener (Tomcat on `*:8080`) answered with 404 while our IPv4-only mlx_lm server sat healthy on 127.0.0.1:8080; health checks even reported the live server unreachable | mlx_lm binds IPv4-only: use `127.0.0.1` (never `localhost`) for every serving URL, link, and health probe; port pre-check binds 127.0.0.1 to match |
| 2026-07-21 | SIGSTOP sent to only the MLX finetune wrapper pid — the wrapper spawns the real `mlx_lm` training subprocess via `subprocess.Popen` without its own process group, so it just inherits whichever group the wrapper was already in; signaling the wrapper alone freezes it but the `mlx_lm` child keeps running the actual GPU work to completion (confirmed on real hardware: 60-iter smoke run finished normally while the "paused" wrapper sat frozen). Same gap for SIGTERM/SIGKILL — the child survives as an orphan | Spawn the wrapper with `tokio::process::Command::process_group(0)` so it leads its own process group (child inherits it), then target `-pid` (the group) for SIGSTOP/SIGCONT/SIGTERM/SIGKILL (D17) |
| 2026-07-21 | `resolve_cli_path`로 바이너리 절대경로만 해석 — colima처럼 **자식 프로세스(limactl)를 PATH로 찾는 도구**는 GUI 앱의 빈 PATH에서 여전히 실패(실기기 재현: `env PATH=/usr/bin:/bin colima status` → fatal, `/opt/homebrew/bin` 추가 시 정상) | 모든 외부 스폰에 보강 PATH env 주입(`external_command` 헬퍼가 `resolve_cli_path` + `.env("PATH", augmented_path())`를 함께 적용; venv python처럼 절대경로를 직접 구성하는 스폰에도 `augmented_path()`를 동일하게 주입) |

## Frontend / Design system

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-07-24 | Tailwind v4 resolves `max-w-sm/md/…` against the **spacing scale** — our DESIGN.md `--spacing-sm/md` (8/12px) shadowed the defaults, so `max-w-sm` compiled to `max-width: 8px` and Korean text rendered one character per line (user-reported "세로 깨짐", chased as a flex bug twice before the compiled CSS was checked) | Named `max-w-*` is forbidden while spacing tokens reuse those names — use arbitrary values (`max-w-[24rem]`); when a layout bug defies flex logic, grep the COMPILED dist CSS for the utility before theorizing |
| 2026-07-25 | A new bottom "terminal dock" rendered three hardcoded log lines ("Colima 가동 준비 완료", "k8s-agent … 8080 수집 중", "포워딩 정상") with no data source, the sidebar fell back to `Apple M4 Pro`/14/20/64GB when the hardware IPC failed, and `KagentOpsView`'s catch block rendered a fully healthy diagnostics report — so an IPC outage looked like a green cluster | Every status string renders from state the component already subscribes to; a failed fetch renders the error, not a plausible default. Same rule as the 07-24 fake-DAG row, now applied to status text as well as progress (D22) |
| 2026-07-25 | `SpecializedModelGuideCard` (314 lines) was a near-verbatim copy of `KagentOpsView` — same IPC calls, same fabricated fallback — mounted inside `PipelineView`, so one fix would have had to be made twice and the two panels could disagree on the same cluster | Deleted the duplicate; the dedicated kagent Ops tab owns that surface. Before adding a panel, grep for an existing component calling the same IPC command |
| 2026-07-24 | Chat playground sent `model: 'mlx'` — mlx_lm.server treats an unknown model name as a HuggingFace repo id and phones home (401/404 from HF mid-chat) | The `model` field must be the exact id the server reports at `/v1/models` (= serving.model_path); pass it as a prop, never a made-up label |

## Kubernetes / MLOps stack

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-07-20 | `ExternalName` Service manifest declared `ports` — ExternalName is a DNS CNAME alias only, no port proxying | Omit `ports`; clients dial `host.lima.internal:8080` directly (D10) |
| 2026-07-20 | Referenced a nonexistent tool "mlx-serve" | The real tool is `mlx_lm.server` (mlx-lm package) or `llama-server` (D12) |
| 2026-07-20 | Guide cited decision IDs (D6/D8/D9) that didn't match the canonical registry in `docs/03-mvp-design.md` §4 — two decisions weren't even registered | Registry is canonical; cite it verbatim, register missing decisions (D11 memory pressure, D12 serving tool) instead of inventing numbers |
| 2026-07-20 | docs/03 reference code forwarded only 2 of the 3 D1 ports (MinIO Console 9001 missing); the implementation lane faithfully copied the defect, and the UI linked to the unforwarded port | Reference code in docs is spec too — when a doc lists N required values, grep the implementation for ALL N, not just the code path |
| 2026-07-20 | UI hardcoded "6 CPU / 12GB" VM default, contradicting the 16GB device profile | Auto-size from detected RAM per D4 profile table |
| 2026-07-20 | Assumed forwarded endpoints (5001/8333/8888) stay reachable — `kubectl port-forward` children die with their parent, so after verification cleanup the URLs went dark and looked like an app bug | Port-forwards are process-bound: reachable only while the app's forward (or a manual `nohup kubectl port-forward`) is alive; always state which process owns a forward when reporting URLs as working |
| 2026-07-25 | `make forward` port-forwarded the kagent UI to host **8080** — the port D1 assigns to model serving and that `suggest_serving_port` hands out first — so starting serving and kagent UI together would have silently collided; docs 11/13/16/17 meanwhile described serving as `:8081`, contradicting D1 | kagent UI forwards to **8090** (`8090:8080`); a new service claiming a host port must be checked against the D1 table AND the `suggest_serving_port` range in the same change (D1 amended) |
| 2026-07-23 | Bare `pip install lm-eval` (no extras) installs fine but `local-completions`/`local-chat-completions` model types raise `ModuleNotFoundError: tenacity` at first use — the `api` extra (`tenacity`, `tiktoken`) isn't a base dependency even though it's required for the exact HTTP-eval path this app needs | `setup_eval_env` installs `lm-eval[api]`, never bare `lm-eval` (D20) |

## Process / Orchestration

| Date | Mistake | Fix |
|------|---------|-----|
| 2026-07-20 | Re-instructed an unresponsive worker lane AND spawned a replacement lane for the same files — the original resumed and both nearly edited the same three docs concurrently | On lane silence, send one follow-up and wait with a timeout; never run two lanes on overlapping files (stop one first) |
| 2026-07-24 | A takeover session shipped a "visual DAG pipeline" whose first two stages were setTimeout theater with hardcoded counts (web=14/HF=100), silently ignored the selected source, and never called the hardware-verified backend engine — 3 registered handlers sat dead while the UI looked fully functional | Fake completion detector: after wiring UI to IPC, cross-check `generate_handler!` vs frontend `invoke()` in BOTH directions (dead handlers = unwired features); any staged progress must come from real backend status, never client-side delays |
| 2026-07-24 | The orchestrator itself repeated the dual-track mistake: sent an SSRF-fix instruction to the owning lane, got no reply, then edited `data_ingest.rs` directly — the lane resumed on the queued message mid-edit, observed a broken half-state, and had to halt and ask who owns the file (mistakes-log row from 07-20 predicted exactly this) | Pick ONE track per file and say so explicitly: either wait for the lane (with a timeout) or send "stand down, I'm taking this file" BEFORE editing; a queued instruction to an idle lane is a future edit, not a dead letter |
| 2026-07-25 | The `scripts/e2e/` suite was theater end to end: step 1 substituted a mock string when serving was down (writing fake training data), step 2 wrote 43 bytes of dummy "LoRA weights" and printed invented metrics (`train_loss=0.241`, "Registry 등록 성공"), step 3 printed a kagent diagnosis that was a literal in the source, and the bash runner printed `🎉 SUCCESS` unconditionally because the Python steps always exited 0 — `make test-e2e` would have been cited as passing evidence | A verification script may only print what it observed; anything it cannot do, it declares out of scope and skips (step 2 now checks MLflow/SeaweedFS reachability and says training happens in MLX Studio). Every step exits non-zero on failure and the runner stops at the first failure and names it (D24) |
| 2026-07-24 | Takeover-session commits were merged with only `cargo check`/`npm run build` — clippy(-D warnings) failed, DESIGN.md token rules and docs §4.1 sync were skipped for 20 commits | Commits from any other session/tool get the FULL gate before building on them: cargo test+clippy, tsc, build, design lint, token grep, IPC cross-check, docs sync |
